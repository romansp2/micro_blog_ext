<%= form_with(model: post_option, local: true) do |form| %>
  <% if post_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post_option.errors.count, "error") %> prohibited this post_option from being saved:</h2>

      <ul>
      <% post_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :posts_id %>
    <%= form.text_field :posts_id, id: :post_option_posts_id %>
  </div>

  <div class="field">
    <%= form.label :option_key %>
    <%= form.text_field :option_key, id: :post_option_option_key %>
  </div>

  <div class="field">
    <%= form.label :option_value %>
    <%= form.text_area :option_value, id: :post_option_option_value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
