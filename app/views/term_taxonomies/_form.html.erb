<%= form_with(model: term_taxonomy, local: true) do |form| %>
  <% if term_taxonomy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(term_taxonomy.errors.count, "error") %> prohibited this term_taxonomy from being saved:</h2>

      <ul>
      <% term_taxonomy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :term_id %>
    <%= form.text_field :term_id, id: :term_taxonomy_term_id %>
  </div>

  <div class="field">
    <%= form.label :taxonomy %>
    <%= form.text_field :taxonomy, id: :term_taxonomy_taxonomy %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :term_taxonomy_description %>
  </div>

  <div class="field">
    <%= form.label :parent %>
    <%= form.text_field :parent, id: :term_taxonomy_parent %>
  </div>

  <div class="field">
    <%= form.label :count %>
    <%= form.text_field :count, id: :term_taxonomy_count %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
